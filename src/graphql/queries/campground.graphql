query Campground($id: Int!) {
  campground(id: $id) {
    id
    name
    location
    image
    points
    createdAt
    updatedAt
    creatorId
    voteStatus
    creator {
      id
      username
    }
  }
}
