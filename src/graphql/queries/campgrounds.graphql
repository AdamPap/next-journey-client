query Campgrounds($limit: Int!, $cursor: String) {
  campgrounds(limit: $limit, cursor: $cursor) {
    hasMore
    campgrounds {
      id
      name
      location
      points
      voteStatus
      createdAt
      updatedAt
      creator {
        id
        name
        username
      }
    }
  }
}
